## Armux Terminal v0.11.8

### 로컬 터미널 문제 셋 (맥에서 발견)

셋 다 뿌리가 같았습니다 — 로컬 세션을 "SSH 가 아니니 안 된다" 로 막아 두었던
곳들입니다.

**① 한글이 깨지던 문제.** Finder/독으로 띄운 GUI 앱은 언어 환경(LANG)이 비어
있어서, 그걸 그대로 물려받은 로컬 셸이 한글을 다루지 못했습니다. 로컬 셸을 띄울
때 UTF-8 로케일을 채워 줍니다(이미 설정돼 있으면 건드리지 않습니다).

**② 파일 탭이 없던 문제.** 로컬 그룹은 "SFTP 가 없다" 며 📁 탭을 숨기고
있었습니다. 숨기는 대신 되게 만들었습니다 — 같은 탐색기가 이 PC 의 파일시스템을
그대로 보여 줍니다. 목록·이동·새 폴더·이름 변경·삭제·파일 열기/저장·
내려받기/올리기 전부 원격과 동일하고, 파일 뷰어도 그대로 씁니다.

**③ 하단바에 Claude/Codex 사용량이 안 나오던 문제.** 사용량 조회와 상태 훅
설치가 SSH 명령 실행에 묶여 있었습니다. 로컬에서도 같은 조회를 이 PC 에서
돌립니다(로그인 셸로 돌려 GUI 의 얇은 PATH 에서도 claude 를 찾습니다). 덤으로
로컬 Claude 에도 상태 훅이 설치되어 탭 스피너/느낌표가 붙습니다.

### 접속 시도를 Ctrl+C 로 중단

인터넷이 끊겼다 돌아온 직후에는 연결 시도가 죽은 경로에 매달려 "접속 중…" 인 채
시간 초과까지 기다리는 일이 있습니다. 이제 **접속 시도 중이거나 자동 재접속을
기다리는 판에서 `Ctrl+C`(맥은 `⌘C` 도)** 를 누르면 시도를 그 자리에서 끊고
"Enter 를 누르면 다시 접속합니다" 상태로 돌아갑니다. Enter → 또 매달리면 또
Ctrl+C, 반복할 수 있습니다.

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
