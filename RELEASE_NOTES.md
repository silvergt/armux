## Armux Terminal v0.10.6

### mac 한글 입력이 깨지던 문제 수정

문장 중간이나 앞 줄로 커서를 옮겨 한글을 치면 **"너너너", "츠츠츠" 처럼 무엇을
쳐도 앞서 친 글자가 반복**되던 문제를 고쳤습니다. 영어는 멀쩡했던 이유까지
포함해 원인을 짚었습니다.

한글은 조합이 끝날 때 터미널의 "숨은 입력칸" 에 쌓인 내용의 일부를 잘라내서
보내는 방식입니다. 그 입력칸이 Enter 를 누를 때까지 계속 쌓이는데, 커서가
한 번이라도 끝에서 벗어나면 잘라낼 자리가 어긋나 옛 글자가 나갔습니다.
이제 조합이 끝날 때마다 그 입력칸을 비워서, 어긋날 여지 자체를 없앴습니다.

### 파일 드래그 · 스크린샷 붙여넣기 (새 기능)

서버에서 돌아가는 Claude Code 에 **내 PC 의 파일을 건넬 수 있습니다.**

- **터미널 판에 파일을 끌어다 놓으면** 서버로 올리고 그 경로를 프롬프트에
  적어 줍니다. 그대로 물어보면 됩니다.
- **스크린샷을 복사한 뒤 `Ctrl/⌘+V`** 를 누르면 그림을 서버로 올리고 경로를
  적어 줍니다. 클립보드가 글자면 지금까지처럼 그대로 붙여넣습니다.
- **AI 채팅에 파일을 끌어다 놓으면** 그 내용이 다음 질문에 첨부됩니다.

올린 파일은 서버의 `~/.armux/dropped` 에 들어가고 **한 시간 뒤 자동으로
지워집니다.** 지우는 대상은 그 폴더 바로 아래의 일반 파일뿐입니다 — 하위 폴더,
그 폴더 밖의 파일, 심볼릭 링크는 건드리지 않습니다.

### AI 채팅 창 크기 조절 · 전체보기

`Ctrl/⌘+K` 팝업의 **왼쪽·위쪽 가장자리와 왼쪽 위 모서리를 끌어 크기를 바꿀 수**
있습니다. 크기는 기억해 두고 다음에 열 때도 그대로 씁니다. 머리의 **⛶** 로
화면 가득 넓혔다가 되돌릴 수 있습니다.

### 포트 전달을 번호 직접 입력 방식으로

서버에서 열린 포트를 훑어 목록으로 보여 주던 것을 없앴습니다. 작업자
프로세스가 많은 서버에서는 수십 개가 쏟아져 정작 찾는 개발 서버가 묻혔습니다.

⇄ 를 누르면 **번호를 넣어 전달하고, 전달 중인 것을 [열기]·[끊기]** 하는 작은
창이 뜹니다.

### 안정성

- **SSH 연결이 험하게 끊길 때 앱이 통째로 죽을 수 있던 문제.** 터미널 스트림에
  오류 처리가 없어 예외가 그대로 올라왔습니다. 이제 그 판만 끊긴 것으로
  처리합니다.
- 느린 서버에서 조회가 반복 실패하면 연결 채널이 쌓여 사용량·포트·AI 가
  한꺼번에 멈추던 문제.
- 예상 못 한 오류로 앱이 내려가지 않도록 방어막을 두고, 조용히 실패하던 것을
  하단바에 보여 줍니다.
- 파일 탐색기를 열고 닫을 때마다 메모리가 조금씩 새던 문제.
- 판에서 연 AI 채팅에서 **열어 둔 파일을 첨부할 수 없던 문제** (팝업에서는
  되던 것).
