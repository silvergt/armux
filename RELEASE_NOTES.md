## Armux Terminal v0.11.7

### 맥 로컬 터미널 — 진짜 원인을 고쳤습니다

v0.11.6 에서 같은 문제(`posix_spawnp failed`)를 고쳤다고 했지만 **원인 진단이
틀렸고, 고쳐지지 않았습니다.** 릴리즈된 맥 패키지의 목차를 직접 열어 확인한 결과,
문제의 보조 실행 파일(spawn-helper)은 이미 올바른 위치에 있었습니다 — 다만
**실행 권한이 없는 채(644)로** 들어 있었습니다. node-pty 가 npm 으로 배포하는
맥 프리빌드 자체가 권한 없이 나오는 문제입니다. 실행 권한이 없는 파일을 실행하려
하니 그 오류가 났던 것입니다.

이번에는 두 겹으로 고쳤습니다.

1. **앱이 스스로 바로잡습니다** — 로컬 터미널을 띄우기 직전에 spawn-helper 의
   실행 권한을 확인하고, 없으면 부여합니다. 앱만 갱신하면 됩니다.
2. **패키지도 바르게 만듭니다** — 빌드 단계에서 권한을 고쳐, 패키지에 들어가는
   파일 자체가 처음부터 실행 가능하게 했습니다.

릴리즈된 맥 패키지의 파일 권한 비트로 두 버전(0.11.5 · 0.11.6) 모두 644 였음을
확인했고, 앱 쪽 바로잡기가 644 → 755 로 고치는 것과 로컬 터미널이 뜨는 것을
검증했습니다.

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
