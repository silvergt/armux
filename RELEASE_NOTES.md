## Armux Terminal v0.9.16

### ✳ AI 질문 (새 기능)

터미널 판에서 **`Ctrl/⌘+K`** (또는 도움 → AI 질문)를 누르면 **독립된 AI 질문
창**이 열립니다. 터미널 화면을 전혀 가리지 않으며, 다른 모니터로 옮기거나
📌 버튼으로 항상 위에 고정할 수 있습니다.

- 누른 판의 **화면 내용(또는 드래그로 선택한 글자)이 자동으로 함께 전송**됩니다.
  에러가 뜬 화면에서 바로 "이 에러 왜 나?"라고 물으면 됩니다.
- 답변은 **그 서버에 로그인된 Claude 계정**(`claude -p`)으로 실행됩니다.
  API 키도, 추가 설치도 필요 없습니다. 사용량은 해당 계정에서 차감됩니다.
- 이어지는 질문은 같은 대화로 계속되어 맥락이 유지됩니다.
  Ctrl+K 를 다시 누르면 새 화면 컨텍스트로 새 대화가 시작됩니다(구분선 표시).

### 로컬 터미널 (새 기능)

접속 창 하단에 **"로컬 터미널"** 버튼이 생겼습니다. SSH 없이 이 PC 의 기본
셸(Windows: PowerShell, macOS: zsh)을 여는 정식 터미널입니다. 분할·서브탭
추가·세션 복원 모두 동일하게 동작합니다.

### 웹 판에 크롬식 탭

판 안 브라우저가 크롬처럼 **탭 여러 개**를 가집니다. `+` 새 탭, 클릭 전환,
✕/휠 클릭 닫기, **새 창 링크(target=_blank)는 새 탭으로** 열립니다. 모든 탭이
로그인 세션을 공유하고, 앱을 껐다 켜면 탭 구성이 복원됩니다.
즐겨찾기 버튼은 별 아이콘(☆/★)만 표시합니다.

### 상태 표시를 이벤트 방식으로 (스피너 제거)

"작업 중" 스피너를 없애고 **확인이 필요할 때만 초록 느낌표**를 표시합니다.
끝 신호가 유실되는 상황(ESC 중단 등)에서 스피너가 계속 돌던 문제가 구조적으로
사라졌습니다. 안 보는 사이 작업이 끝나면 느낌표, 입력·권한 대기도 느낌표,
탭을 열어 확인하면 해제 — 서브탭과 메인탭에 집계 표시됩니다.

### 고친 것

- 파일 내 검색(Ctrl/⌘+F)에서 글자를 입력하면 포커스가 본문으로 튀던 문제 수정.
  포커스는 검색창에 유지되고, 찾은 자리는 노란 강조로 표시됩니다(현재 결과는
  진한 노랑).
- 폴더뷰 판 위로 다른 판을 드래그하면 폴더에 넣으려고만 하고 분할이 안 되던
  문제 수정.
