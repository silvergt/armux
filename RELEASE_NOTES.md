## Armux Terminal v0.11.3

### 세션 녹화가 걸린 서버에서 스피너가 아예 안 켜지던 문제

로그인하면 "Your session is being recorded" 가 뜨는 서버에서는 어떤 작업을 해도
탭에 스피너가 켜지지 않았습니다. Claude 도, 셸 명령도 마찬가지였습니다.

그 서버는 `sshd → 녹화기 → 셸 → tmux` 순으로 겹쳐 있습니다. 앱은 tmux 를 "이
판과 같은 터미널에 붙은 클라이언트" 로 찾았는데, 녹화기가 자기만의 안쪽 터미널을
만들고 그 안에서 tmux 가 돌기 때문에 찾지 못했습니다. 그래서 판이 "녹화기 하나
떠 있음" 으로만 보였고, 그건 전체화면 앱으로 분류돼 아무 판정도 하지 않았습니다.

이제 tmux 를 **프로세스 계통**으로 찾습니다 — tmux 클라이언트의 조상을 거슬러
올라가 이 판의 셸이 나오면 그 세션입니다. 터미널이 몇 겹이든 상관없습니다.

녹화기 안에서 tmux 를 붙인 구조로 실제 Claude 를 돌려 확인했습니다: 작업 중 스피너,
도구 실행 중 거짓 느낌표 없음, 완료 느낌표, ESC 로 끊으면 꺼짐. 셸 명령의 스피너도
정상입니다. 이 구조를 검증하는 시험을 새로 두었습니다.

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
