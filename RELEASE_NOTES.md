## Armux Terminal v0.11.11

### 밝은 모드 — 이제 앱 전체가 밝아집니다

**설정 ▸ 모양 ▸ 화면 색** 을 밝게로 두면(맥은 `⌘,`), 터미널과 상하단바뿐 아니라
**앱의 모든 화면**이 함께 밝아집니다.

- 메모 목록·편집, 파일 탐색기·파일 열기, 퀵 AI·AI 채팅 판(마크다운·코드 블록 포함),
  퀵메모, 설정, 프로그램 정보, 업데이트, 도움말, 새 접속 창, 포트 전달, 뽀모도로,
  메뉴·우클릭 메뉴, 웹페이지 판 도구줄, 새 판 고르기 화면까지
- 모달 뒤 가림막은 그대로 어둡게 두어 창이 또렷하게 떠 보입니다
- 어둡게로 돌리면 전부 원래대로 돌아옵니다

(웹페이지 판 안에 뜨는 실제 웹사이트는 그 사이트 자체 색을 씁니다)

### 밝은 모드에서 tmux 상태줄이 안 보이던 문제

tmux 기본 상태줄은 "초록 바탕 + 검은 글씨" 인데, 밝은 팔레트의 초록이 진해서 글자가
묻혔습니다. 밝은 모드에서는 **글자와 바탕의 명암 차이가 너무 작으면 글자 밝기를
자동으로 조정**합니다 — 상태줄 글씨가 흰색으로 또렷하게 보입니다. 다른 프로그램의
대비 낮은 색 조합도 같이 좋아집니다.

### Claude Code 색감이 이상하게 보일 때 (tmux 안에서)

tmux 는 기본 설정에서 24비트 색을 **256색으로 낮춰** 전달합니다. 그래서 Claude 의
테마 색(특히 옅은 색이 많은 라이트 테마)이 뭉개져 보입니다. 서버의 `~/.tmux.conf` 에
두 줄을 넣으면 원래 색 그대로 나옵니다:

```
set -as terminal-features ",xterm-256color:RGB"
set-environment -g COLORTERM truecolor
```

`tmux source-file ~/.tmux.conf` 후 tmux 에 다시 붙고, Claude 를 새로 켜면 적용됩니다.

- 로컬 터미널은 앱이 `COLORTERM=truecolor` 를 알려 주도록 했습니다
  (Finder·Dock 에서 켠 앱은 이 값이 없어 256색으로 칠해졌습니다)

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음 — tmux 색 설정은 원하실 때 직접).
