## Armux Terminal v0.9.15

### 파일뷰

**바로 편집** — "편집" 버튼을 없앴습니다. `.py` · `.sh` · `.yaml` · `.json` 같은
파일을 열면 곧바로 수정할 수 있고, 그러면서 구문 강조 색상은 그대로 유지됩니다.

**줄 번호** — 왼쪽에 줄 번호가 표시됩니다. 번호와 코드가 어긋나지 않도록 자동
줄바꿈 대신 가로 스크롤을 씁니다. Tab 은 들여쓰기로 동작합니다.

**파일 안에서 찾기** — `Ctrl/⌘+F` 로 검색바가 열립니다. 결과 개수 표시,
Enter · Shift+Enter 로 다음 · 이전 이동, Esc 로 닫기. 드래그해 둔 글자가 있으면
검색어로 자동 입력됩니다.

**여는 방식 변경** — 파일이 새 서브탭이 아니라, 지금 보고 있는 서브탭에서 기준 판
**오른쪽에 새 판**으로 열립니다. 터미널과 파일을 나란히 놓고 볼 수 있습니다.

### 판을 끌어서 분할

판 상단바를 끌어 다른 판의 **위 · 아래 · 좌 · 우 가장자리**에 놓으면 그 방향으로
분할되어 들어갑니다. 가운데나 **상단바**에 놓으면 지금까지처럼 두 판의 자리를
바꿉니다. 끌고 있는 동안 어디에 붙을지 반투명 막대로 미리 보여 줍니다.

### 웹 판

- `☆ 즐겨찾기` → **`⌂ 홈`** (기능 동일)
- `★ 추가` → **`☆ / ★ 즐겨찾기` 토글**. 지금 페이지가 목록에 있으면 꽉 찬 노란 별,
  없으면 빈 별. 눌러서 추가 · 해제합니다.
- **인증서를 확인할 수 없는 사이트가 하얀 화면으로만 뜨던 문제 수정.** 자체 서명
  인증서를 쓰는 사내 서버 등을 이제 열 수 있습니다. 브라우저처럼 한 번 물어보고,
  허용하면 그 호스트만 열어 줍니다(앱을 끄면 초기화). 그 밖의 로딩 실패도 빈 화면
  대신 이유와 재시도 · 크롬에서 열기 버튼을 보여 줍니다.

### 상태 아이콘

**판을 여러 개로 쪼갰을 때 스피너가 잡히지 않던 문제 수정.** 판이 짧아지면 Claude
작업줄이 검사 범위를 벗어나면서 스피너가 꺼지곤 했습니다. 이제 입력 상자 · 단축키
힌트 같은 Claude 화면의 흔적까지 넓게 보고, 그마저 사라졌을 때만 끕니다.

서브탭과 메인탭은 지금까지처럼, 안에 있는 판 중 **하나라도** 작업 중이면 스피너를,
**하나라도** 확인이 필요하면 초록 느낌표를 표시합니다.

---

> **설치 시 경고가 뜨는 경우** — 코드 서명 인증서를 쓰지 않아 Windows SmartScreen 이
> "알 수 없는 게시자" 경고를 띄웁니다. **추가 정보 → 실행** 을 누르면 정상 설치됩니다.
> macOS 는 `xattr -dr com.apple.quarantine /Applications/Armux\ Terminal.app` 으로
> 해제할 수 있습니다.
