## Armux Terminal v0.10.4

### AI 채팅이 메인탭마다 따로

`Ctrl/⌘+K` 로 여는 AI 채팅이 이제 **메인탭(서버)마다 하나씩** 있습니다.
탭마다 서버가 다르니 대화도 따로 가는 것이 맞습니다.

- **대화 내용이 탭마다 분리됩니다.** 탭1 에서 하던 이야기가 탭2 에 섞이지
  않습니다. 팝업 머리에 그 탭의 서버 이름이 보입니다.
- **열려 있는지도 탭마다 따로입니다.** 탭1 에서는 열어 두고 탭2 에서는 닫아 둘
  수 있습니다. 탭을 옮기면 그 탭의 팝업만 나타나고, 다른 탭 것은 감춰만
  두므로 하던 대화도 기다리던 답도 그대로입니다.
- **다른 탭에서 답이 도착하면 그 메인탭에 초록 느낌표**가 붙습니다.
  자리를 비운 사이 어느 서버에서 답이 왔는지 바로 알 수 있습니다.
- 하단바 AI 단추(켜짐 · 기다리는 중 · 도착함)도 보고 있는 탭 기준입니다.

### 고친 것

- **대화 중 다른 서버로 옮기면 "오류가 났습니다" 가 뜨던 문제.**
  이어 말하기 정보는 그 서버에 있는 것이라, 다른 서버로 보내면 찾지 못해
  실패했습니다. 이제 대화는 시작한 서버로 계속 이어지고, 그래도 이어가지
  못하는 경우에는 새 대화로 자동으로 다시 물어본 뒤 그 사실을 알려 줍니다.
  Claude 와 Codex 양쪽 모두에서 확인했습니다.
- 서버가 바뀌어 새 대화로 넘어갈 때 방금 붙인 📎 첨부가 사라지던 문제.
