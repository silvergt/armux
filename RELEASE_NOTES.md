## Armux Terminal v0.10.0

### ✳ AI 채팅 판 (새 기능)

판 상단의 **전환** 메뉴에 **"✳ AI 채팅"** 이 추가됐습니다. claude.ai 처럼
대화형으로 주고받되, 실제 실행은 접속한 서버의 `claude -p` 로 이뤄집니다.
API 키도, 추가 설치도 필요 없습니다.

- 대화 맥락이 계속 이어집니다. **새 대화** 버튼으로 언제든 초기화할 수 있고,
  이전 기록은 구분선과 함께 화면에 남습니다.
- **📎 컨텍스트** 버튼으로 같은 서브탭의 **다른 판 내용을 첨부**할 수 있습니다.
  옆 판의 터미널 화면은 물론, **웹페이지 본문**도 그대로 붙습니다.
  "이 문서 요약해 줘", "이 에러 원인이 뭐야?" 를 판을 옮기지 않고 물어보세요.
- 터미널 판과 나란히 분할해 두면, 명령을 실행하면서 바로 옆에서 물어볼 수
  있습니다.

### 🧠 사고 과정 실시간 표시

AI 질문(`Ctrl/⌘+K`) 창과 AI 채팅 판 모두, 이제 **"생각 중…" 대신 답변이
실시간으로 타이핑되듯 채워집니다.**

- Claude 가 사고 과정(thinking)을 내보내면 **🧠 사고 과정** 접이식 상자에
  실시간으로 흐릅니다. 답변이 끝나면 자동으로 접히고, 눌러서 다시 펼쳐
  볼 수 있습니다.
- 파일을 읽거나 명령을 실행하는 등 **도구 실행 단계**도 `⚙` 로 함께 표시돼,
  지금 무엇을 하고 있는지 보입니다.
- 참고: 사고 과정은 Claude 가 실제로 내보낼 때만 표시됩니다. 짧고 단순한
  질문에는 생략될 수 있으며, 이 경우에도 답변 스트리밍은 그대로 동작합니다.

### 고친 것

- **macOS 한글 입력 버그 수정.** 이미 쓴 문장 중간으로 커서를 옮겨 글자를
  덧붙이면 `지지지지`, `추추추추` 처럼 같은 글자가 반복 입력되던 문제를
  고쳤습니다. 조합(IME) 중인 입력을 터미널이 가로채 이중으로 보내던 것이
  원인으로, 이제 조합이 확정된 뒤 정확히 한 번만 전송됩니다.
- AI 질문 창이 별도 윈도우일 때 응답 스트리밍이 도착하지 않던 문제 수정.
