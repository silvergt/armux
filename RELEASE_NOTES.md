## Armux Terminal v0.11.12

### 이모지가 있는 줄의 링크가 클릭되지 않던 문제

`🐊 caiman 리서치 플로우 — ⧉ https://…` 처럼 **이모지가 앞에 있는 줄** 은 링크가
잡히지 않았습니다(밑줄도 안 생기고 클릭도 안 됨).

이모지는 화면에서는 글자 하나지만 프로그램 안에서는 두 자리를 차지합니다. 그래서
링크 위치가 이모지 하나마다 한 칸씩 밀렸고, 밀린 끝이 줄 길이를 넘으면 armux 가
"이 줄 밖의 링크" 로 보고 통째로 버렸습니다. 줄이 짧아 살아남은 경우에도 밑줄과
클릭 범위가 한 칸씩 어긋나 끝 글자를 누르면 반응이 없었습니다.

`⧉` 같은 기호나 한글에는 없던 문제입니다 — 이모지에만 생겼습니다.

### 맥 로컬 터미널에서 Claude/Codex 사용량이 안 뜨던 문제

- **Claude** — 맥은 로그인 토큰을 파일이 아니라 **키체인**(`Claude Code-credentials`)에
  둡니다. 파일만 찾다가 "로그인 안 됨" 으로 판정했습니다. 이제 키체인에서도 읽습니다
  (처음 한 번 키체인 접근 허용을 물을 수 있습니다 — "항상 허용")
- **Codex** — 조회에 쓰던 `timeout` 명령이 **맥에는 없어서** 실행 자체가 실패했습니다.
  없으면 없는 대로 동작하게 고쳤습니다

### 윈도우 로컬 터미널에서도 사용량 조회

윈도우는 로컬 명령을 PowerShell 로 돌리는데 조회 스크립트가 리눅스·맥용이라 애초에
동작할 수 없었습니다. 윈도우에서는 앱이 직접 조회합니다.

- Claude 는 로그인 파일을 읽어 사용량을 받아옵니다(시스템 프록시 설정을 따릅니다)
- Codex 는 PATH 와 npm 전역 폴더(`%APPDATA%\npm`)에서 `codex.exe`·`codex.cmd` 를
  찾아 직접 물어보고, 끝나면 딸린 프로세스까지 정리합니다
- 토큰은 전과 같이 PC 밖으로 나가지 않습니다

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
