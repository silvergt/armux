## Armux Terminal v0.11.13

### 분할한 판에서 드래그 선택(복사)이 안 되던 문제

두 번째·세 번째 판부터는 글자를 끌어도 선택이 되지 않아 복사를 할 수 없었습니다.

판을 누르면 armux 가 그 판의 크기를 다시 맞추는데, 계산이 두 단계였습니다 —
먼저 한 줄 늘렸다가(17줄) 화면 밖으로 나가는지 재서 도로 줄였습니다(16줄).
**터미널은 크기가 바뀌면 진행 중인 선택을 지웁니다.** 그래서 누르는 순간 선택이
시작됐다가 곧바로 지워졌습니다. 첫 판만 멀쩡했던 것은 계산이 딱 맞아떨어져
크기 변경이 없었기 때문이고, 분할하면 헤더·칸막이 때문에 거의 항상 어긋납니다.

이제 목표 크기를 먼저 구해 **실제로 줄·칸 수가 달라질 때만** 바꿉니다. 판을 눌러도
크기 변경이 일어나지 않습니다. 서버로 가는 창 크기 통지도 줄어 tmux 가 쓸데없이
다시 그리지 않습니다.

- 분할 직후 새 판이 한 줄 모자란 채로 남던 것도 같이 고쳤습니다
  (분할은 바깥 상자 크기를 바꾸지 않아 크기 감시가 깨어나지 않았습니다)

### 여러 줄로 잘린 긴 주소의 마지막 줄을 빼먹던 문제

Claude 로그인 주소처럼 긴 URL 은 tmux·TUI 가 제 폭대로 잘라 여러 줄에 그립니다.
armux 는 그 줄들을 다시 이어 붙이는데, **마지막 꼬리 줄**은 `%&=?` 나 `/` 가 있을
때만 이어 붙였습니다. OAuth 주소의 끝은 `…&state=S3TV0Fyc27K…Pnc-KI` 같은 무작위
토큰이라, 잘리는 폭에 따라 마지막 줄이 영숫자와 `-_` 만 남으면 그 줄을 버렸습니다.
그러면 주소가 잘린 채 열리고, 마지막 줄은 아예 눌리지 않았습니다.

이제 영숫자·기호로만 된 조각도 꼬리로 인정합니다. 링크 바로 뒤에 오는 낱말까지
끌어오지 않도록, 숫자나 기호가 섞인 4자 이상만 붙입니다.

- 5~10줄로 잘린 경우 **어느 줄을 눌러도** 주소 전체가 열립니다

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
