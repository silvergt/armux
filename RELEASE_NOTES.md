## Armux Terminal v0.10.11

### 세션 녹화가 걸린 서버에서 스피너가 안 꺼지던 문제

로그인하면 `ATTENTION! Your session is being recorded!` 같은 안내가 뜨는 서버가
있습니다. 이런 서버는 셸을 **감싸는 것**이 하나 끼어 있어서, 포그라운드 명령
이름이 우리가 아는 것과 다르게 나옵니다. 그래서 프롬프트만 떠 있는데도 "작업
중" 으로 판정돼 **스피너가 영영 돌았습니다.** 그 서버에 붙은 탭만 그랬고 나머지
탭은 멀쩡했습니다.

이름에 기대지 않는 방식으로 바꿨습니다.

- 그 터미널의 **포그라운드 프로세스 그룹이 셸 자신이면 프롬프트가 떠 있는
  것**입니다. 셸이 무엇이든(녹화 래퍼든 낯선 셸이든) 통합니다.
- 녹화 래퍼가 **새 터미널을 여는 경우에는 그 안쪽까지 따라 들어갑니다.** 못 보고
  포기하는 것이 아니라 제대로 봅니다 — **녹화 세션 안에서 돌리는 명령도 정상적으로
  스피너가 뜹니다.**

| 상황 | 표시 |
| --- | --- |
| 녹화만 걸린 채 프롬프트 대기 | 없음 (전에는 스피너가 갇혔습니다) |
| 녹화 세션 **안에서** 명령 실행 | 스피너 |
| `sudo -i` 로 연 루트 셸 | 없음 |
| 그 루트 셸 안에서 명령 실행 | 스피너 |

### Codex 도 Claude Code 와 똑같이

Codex 는 완료돼도 초록 느낌표가 안 뜨고, 생각 중에도 스피너가 안 떴습니다.
이제 **켜져만 있으면 점 · 생각 중이면 스피너 · 끝나면 초록 느낌표**로 Claude Code
와 동일하게 동작합니다.

원인이 두 가지였습니다.

**Codex 는 "시작" 을 알려 주지 않습니다.** 알림 이벤트가 완료와 승인 대기뿐입니다.
그래서 생각 중인지는 화면의 작업 표시(`◦ Working (3s • esc to interrupt)`)로
판단하게 했습니다.

**완료 알림이 "작업 중 → 대기" 로 바뀔 때만 걸려 있었습니다.** Codex 는 시작을
알려 주지 않으니 그 변화가 생기지 않아 느낌표가 영영 안 떴습니다. 이제 완료
신호는 시작을 못 봤더라도 그 자체로 알립니다.

실제 Codex 로 확인한 결과입니다.

| 상황 | 표시 |
| --- | --- |
| 켜져만 있음 | 점 |
| 생각 중 | 스피너 (곧바로) |
| 완료 — 그 화면을 보고 있었음 | 점 (성가시게 하지 않습니다) |
| 완료 — **안 보이는 tmux 창** | 초록 느낌표 (1.4초 만에) |

알아 두실 점: **Codex 는 켜지면서 5초쯤 실제로 준비 작업을 합니다.** 그동안은
스피너가 돕니다.

한 가지 한계 — 안 보이는 tmux 창의 Codex 는 화면을 볼 수 없어서 **생각 중에는
스피너가 뜨지 않습니다.** 완료될 때 초록 느낌표는 정상적으로 뜹니다. Claude Code
는 시작도 알려 주므로 안 보이는 창에서도 스피너가 뜹니다.

---

판정 규칙 61가지를 `npm run test:panestate` 로 검증합니다.
