## Armux Terminal v0.11.6

### 맥에서 로컬 터미널이 안 열리던 문제

메인탭 새로 열기 ▸ 로컬 터미널을 고르면 `posix_spawnp failed` 오류와 함께
터미널이 켜지지 않았습니다.

맥에서 터미널을 띄우는 데 쓰는 보조 실행 파일(`spawn-helper`)이 앱 패키지의
asar 아카이브 안에 갇혀 있었습니다. 아카이브 속 파일은 실행할 수 없어서 그
자리에서 죽은 것입니다. 패키징 규칙을 고쳐 해당 파일이 실행 가능한 위치로
풀리도록 했습니다. 윈도우·리눅스는 이 파일을 쓰지 않아 원래부터 정상이었습니다.

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
