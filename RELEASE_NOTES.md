## Armux Terminal v0.11.4

### 1분 넘긴 Claude 작업에서 스피너가 꺼지던 문제

작업 상태줄의 경과 시간은 `(2s` 로 시작해 1분을 넘기면 `(3m 22s`, 한 시간을
넘기면 `(1h 2m 3s` 로 바뀝니다. 앱은 초만 오는 형태만 알아봐서 **1분이 되는
순간부터 상태줄을 못 읽었습니다.** 앱이 붙기 전부터 돌고 있던 Claude(시작 훅을
받은 적이 없는 창)는 화면이 유일한 근거라 그때부터 점으로 떨어졌습니다.

분·시간 형식을 모두 알아봅니다. 답변 본문의 `(30s)` 같은 글머리 줄을 거르는
조건은 그대로입니다.

### 뽀모도로 기본 시간 50분

기본이 25분에서 **50분**으로 바뀌었습니다. 이미 시간을 직접 고른 적이 있으면 그
저장값이 그대로 남습니다 — 뽀모도로 창에서 50 을 고르거나 설정 ▸ `기본값으로`
를 누르면 됩니다.

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
