## Armux Terminal v0.9.12

### Claude 상태 아이콘이 이제 실제로 동작합니다

작업 중 스피너와 완료 후 초록 느낌표가 대부분 뜨지 않던 문제를 고쳤습니다.

**원인** — Claude Code 는 훅을 *제어 터미널이 없는* 자식 프로세스로 실행합니다
(`tty` → `not a tty`). 그래서 훅은 정상 실행됐지만 터미널로 신호를 보내는
`printf ... > /dev/tty` 가 매번 실패했고, 오류 출력마저 버려져 아무 증상 없이
조용히 죽어 있었습니다.

**수정** — 조상 프로세스를 거슬러 올라가 Claude 본체의 터미널 장치를 찾아
그리로 직접 신호를 보냅니다. 리눅스(`/dev/pts/N`)와 macOS(`/dev/ttysNNN`) 모두
지원하고, tmux 안에서는 passthrough 로 감싸 바깥 터미널까지 전달합니다.

**아이콘이 뜨는 규칙**

| 표시 | 의미 |
|---|---|
| 스피너 | Claude 가 작업하는 중 |
| 초록 느낌표 | 작업이 끝났는데 그 판을 보고 있지 않을 때 (확인 필요) |
| 상태 점 | 그 밖의 경우 |

서브탭에 느낌표가 뜨면 메인탭에도 함께 표시되어, 접혀 있는 탭에서도 어느 판이
확인을 기다리는지 알 수 있습니다. 해당 탭을 열면 자동으로 사라집니다.
권한·입력 대기 알림은 보고 있어도 표시됩니다.

**함께 고친 것** — 훅 신호를 한 번 받으면 화면 감지가 영구히 꺼져 아이콘이 멈추던
문제를 수정했습니다. 30분간 훅 소식이 없으면 화면 감지로 자동 복귀하고, 훅이
동작 중이어도 화면에 스피너가 보이면 작업 중으로 인식합니다.

> 훅은 Claude 가 시작할 때 읽습니다. 이 버전 설치 후 **이미 실행 중이던 Claude 는
> 한 번 재시작**해야 정확한 신호를 보냅니다. 그 전까지는 화면 감지가 대신 동작합니다.

### 그 외

- 릴리스 페이지에 변경점 요약이 함께 올라가도록 빌드 과정을 정리했습니다.
