## Armux Terminal v0.11.2

### Claude 스피너·느낌표 오판 수정

**아무것도 안 하는데 스피너가 돌고, 한창 작업 중인데 초록 느낌표가 뜨던 문제**를
고쳤습니다. 원인이 세 가지였습니다.

**① ESC 로 끊으면 스피너가 영영 남음.** Claude Code 는 사용자가 ESC 로 끊으면
완료 훅(Stop)을 보내지 않습니다(사양). 앱은 훅만 믿고 있어서 "작업 중" 이 그대로
남았습니다. 이제 **ESC 는 앱이 직접 봅니다** — 우리 앱을 거쳐 가는 키이므로, 지금
보고 있는 창의 스피너를 그 자리에서 내립니다. 내가 끊은 것이니 느낌표는 올리지
않습니다.

**② 도구가 돌 때마다 "끝났다" 고 오판.** 지난 버전의 녹화 서버 지원이 Claude 의
도구 자식 프로세스(제어 터미널 없이 따로 떼어 실행됩니다)를 "녹화 래퍼가 연 안쪽
터미널" 로 오인해, 거기서 "프롬프트 대기" 라고 판정했습니다. 도구가 돌 때마다
스피너가 껐다 켜지고, 그 전이로 **작업 시작 몇 초 뒤 느낌표**가 올라갔습니다.
제어 터미널이 없는 자식은 따라 내려가지 않습니다.

**③ 답변 본문 때문에 뜨던 느낌표.** Claude 가 답변에 `1. Yes` 나 `Do you want …?`
같은 말을 쓰면, 옛 화면 문구 규칙이 진짜 질문인 줄 알고 느낌표를 올렸습니다.
작업 중에는 그 규칙을 보지 않습니다. 진짜 권한 질문은 훅이 따로 알려 줍니다.

### 어떻게 판정하나

에이전트 창의 "작업 중" 은 **훅이 주인**이고, 아래 셋으로만 꺼집니다.

1. 완료 훅 — Stop, 그리고 새로 받는 **StopFailure**(오류로 끝남) · **SessionEnd**(Claude 종료)
2. **사용자의 ESC** (앱에서 누른 것)
3. 셸 프롬프트로 돌아옴 (Claude 가 죽거나 `/quit`)

**화면이 조용하다고 끄지는 않습니다.** 프로세스가 잠깐 멎거나 상태줄 문구가 바뀌어도
"끝났다" 는 거짓 알림이 나지 않게 하기 위해서입니다. 화면의 작업 상태줄은 켜는
쪽으로만 씁니다(Codex 는 시작 훅이 없어서 필요합니다). 요즘 Claude 의 상태줄
형식(`✶ Working… (2s · …)`)도 이제 알아봅니다.

한계 하나: **앱 밖**(다른 tmux 클라이언트)에서 누른 ESC 는 볼 수 없어 그 창의
스피너가 남습니다. 다음 프롬프트나 Claude 종료 때 정리됩니다.

### 연결이 끊기면 자동으로 다시 붙기 — 설정으로 (기본 끔)

설정 ▸ 동작 에 항목이 생겼습니다. 끄면(기본) 끊긴 자리에 그대로 두고 `Enter` 를
눌러야 다시 붙고, 켜면 판을 죽이지 않고 기다렸다가 스스로 붙습니다. 다시 붙는 것은
새 셸이라 하던 작업이 tmux 안이 아니었으면 이어지지 않기 때문에 기본을 껐습니다.
잠깐 끊긴 정도(10분 이내)는 이 설정과 무관하게 SSH keepalive 가 버텨 줍니다.
"절전에서 깨면 자동 재접속" 은 별개 설정으로 그대로 켜져 있습니다.

### 밝은 테마 제거

v0.11.1 에 넣었던 밝은 테마를 뺐습니다. 설정 ▸ 모양 에는 글자 크기·글꼴·커서·
스크롤백이 남습니다.

---

**서버 쪽 반영**: 새 완료 훅(StopFailure·SessionEnd)은 서버에 다음 접속 때
병합됩니다. 이미 실행 중이던 Claude 는 재시작해야 훅이 적용됩니다.

검증: 진짜 Claude 로 도구 작업·ESC 를 돌리는 시험(`npm run test:agent`)을
새로 두었고, 판정 규칙 83가지(`npm run test:panestate`)를 포함해 전체 회귀를
통과했습니다.
