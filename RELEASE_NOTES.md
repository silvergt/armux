## Armux Terminal v0.10.3

### AI 채팅이 하단바에서 올라오는 팝업으로

`Ctrl/⌘+K` 로 여는 AI 채팅이 이제 판(분할 창)이 아니라 **떠 있는 팝업**입니다.
지난 버전에서는 판에서 직접 연 AI 채팅과 생김새가 같아 구분이 되지 않았습니다.

- 하단바 오른쪽 끝에 작은 초록 **AI** 단추가 생겼습니다. 이 단추를 누르거나
  `Ctrl/⌘+K` 를 누르면 그 위로 채팅이 올라옵니다.
- **화면 배치를 건드리지 않습니다.** 터미널 위에 겹쳐 뜨므로 열고 닫아도
  분할해 둔 창이 흐트러지지 않습니다.
- **닫아도 없애지 않고 감추기만 합니다.** 답을 기다리는 중이었다면 그대로
  계속되고, 다시 열면 이어집니다. 기다리는 동안 AI 단추가 숨 쉬고, 접어 둔
  사이에 답이 도착하면 단추에 노란 점이 붙습니다.
- 대화 상대는 지금 보고 있는 서버입니다. 탭을 옮기면 머리의 이름도 바뀝니다.

판 안에서 쓰는 AI 채팅(판 헤더의 **전환** 또는 새 판 고르기)은 그대로입니다.
이제 둘은 생김새부터 다릅니다.

### 고친 것

- **웹 판이 하얀 화면만 뜨던 문제.** 주소 입력창과 즐겨찾기 목록이 보여야 할
  자리에 빈 흰 화면이 덮여 있었습니다. 이제 시작 화면이 제대로 보이고, 주소창
  왼쪽 **⌂ 홈** 으로 언제든 다시 부를 수 있습니다.
- **Claude 사용량 막대가 안 뜨던 문제.** 서버 한 대가 `api.anthropic.com` 에
  닿지 못하면, 그 서버 때문에 **다른 모든 서버의 막대까지** 함께 사라져
  있었습니다. 이제 서버마다 따로 처리합니다.
  - 아직 사용량을 못 받은 서버는 **5분마다 다시 확인**합니다.
  - 보고 있는 탭뿐 아니라 접속된 모든 서버를 갱신합니다. 탭을 옮기면 바로 뜹니다.
  - 조회에 시간이 오래 걸리는 서버에서 시간 초과로 실패하던 것도 늘렸습니다.
- **사용량 막대와 % 숫자 사이 간격**을 좁히고, 세션 **초기화까지 남은 시간**을
  다시 보이게 했습니다(창이 아주 좁을 때만 접힙니다).
