## Armux Terminal v0.11.10

### 밝은 터미널이면 상단바·하단바도 밝게

**설정 ▸ 모양 ▸ 터미널 색** 을 밝게로 두면, 이제 터미널 화면만이 아니라 그 둘레의
줄도 함께 밝아집니다(맥은 `⌘,`).

- **상단** — 메뉴줄, 메인 탭 줄(메모·시계 포함), 서브탭 줄(뽀모도로 포함).
  선택된 탭은 터미널과 같은 흰색이라 이어져 보입니다
- **하단** — 상태바의 Claude·Codex 사용량 막대, 포트·메모·AI 버튼
- **판 머리줄** — 판마다 위에 붙은 제목·분할·닫기 줄
- **윈도우** — 오른쪽 위 최소화·최대화·닫기 버튼 색도 같이 바뀝니다

설정 창·팝업·접속 창처럼 떠 있는 창은 지금처럼 어둡게 둡니다.
어둡게로 돌리면 전부 원래대로 돌아옵니다.

### 고친 것

- 열려 있는 판을 밝게로 바꾸면 **터미널 맨 아래에 검은 띠**가 남던 문제
  (마지막 줄 아래 여백이 검은색으로 고정돼 있었습니다)

### 맥 설치 스크립트 (이미 반영됨)

Armux 의 로컬 터미널 안에서 설치 명령을 돌리면 "실행합니다…" 뒤에 아무 일도
없고, 다시 켜도 옛 버전이 뜨던 문제를 고쳤습니다. 이제는 설치가 끝나면 Armux 가
스스로 재시작되어 새 버전으로 열립니다(열린 세션 종료 확인 창이 뜨면 '종료').
지금 쓰는 앱이 설치 위치와 다른 곳(dmg 디스크 등)에 있으면 그것도 알려 줍니다.

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/silvergt/armux/main/scripts/install-mac.sh)"
```

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
