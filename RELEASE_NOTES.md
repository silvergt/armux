## Armux Terminal v0.9.13

### 판을 웹페이지 · 메모 · 파일로 전환

판 헤더의 웹 전환 버튼이 **`⇄ 전환` 드롭다운**으로 바뀌었습니다. 이제 판(분할 창)
하나를 골라 그 안에서 바로 열 수 있습니다.

- **웹페이지** — 주소 입력과 즐겨찾기가 있는 시작 화면
- **메모** — 지금까지 상단바 전용 화면에서만 쓰던 메모장
- **파일** — 지금까지 전용 서브탭이나 왼쪽 독에서만 쓰던 파일 탐색기

전환한 뒤에는 드롭다운 맨 위에 **`⌨ 터미널`** 복귀 항목이 생기고, 현재 모드에는
체크 표시가 붙습니다.

**터미널은 사라지지 않습니다.** 감춰 둘 뿐이라 돌아오면 SSH 세션과 실행 중이던
작업이 그대로 살아 있습니다. 판마다 따로 열리므로 좌우로 나눈 두 판에서 서로 다른
메모를 보거나 서로 다른 폴더를 탐색할 수 있고, 이 구성은 다음 실행 때 복원됩니다.

기존 상단바 메모 버튼과 파일 탐색기 서브탭·왼쪽 독은 그대로 쓸 수 있습니다.

### 판 헤더 버튼을 알아보기 쉽게

작고 흐려서 무슨 버튼인지 구분하기 어렵던 문제를 고쳤습니다.

- 다섯 버튼을 뜻이 드러나는 아이콘으로 새로 그렸습니다. 특히 분할 버튼은 상자를
  선으로 나누고 새로 생길 쪽을 채워, 좌우 분할인지 위아래 분할인지 한눈에 보입니다.
- 헤더와 버튼을 키우고(22 → 27px, 18 → 21px) 글자색을 밝게 올렸습니다.
  마우스를 올리면 테두리가 생겨 눌리는 영역도 분명해집니다.
- 모든 버튼에 접근성 라벨을 달았습니다.
