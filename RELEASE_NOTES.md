## Armux Terminal v0.10.5

### ⇄ 포트 전달 (새 기능)

서버에서 띄운 개발 서버를 **내 PC 브라우저에서 바로 열 수 있습니다.**
VS Code 의 포트 전달과 같은 원리이고, 새로 접속하지 않고 이미 붙어 있는 그
SSH 연결에 실어 보냅니다.

하단바 오른쪽 **⇄** 를 누르면:

- 그 서버에서 **듣고 있는 포트를 알아서 찾아** 목록으로 보여 줍니다.
  프로세스 이름(`8332  python3`)도 같이 나오므로 어느 것이 내 개발 서버인지
  바로 알 수 있습니다.
- 고르면 전달이 시작되고 **웹 판이 옆에 함께 열립니다.** 터미널에서 고치고
  옆에서 새로고침하는 흐름이 앱 안에서 끝납니다.
- 원격과 같은 번호로 열고, 그 번호가 이미 쓰이는 중이면 다른 번호로 잡아
  알려 줍니다.
- 전달 중인 것은 목록 맨 위에 ✓ 로 모이고 오른쪽 **✕** 로 중지합니다.
  ⇄ 단추에는 전달 개수가 표시됩니다.
- 목록에 없는 포트는 **직접 번호를 넣어** 전달할 수 있습니다.

**바깥에는 열리지 않습니다.** 전달 포트는 `127.0.0.1` 에만 열리므로 같은
네트워크의 다른 기기가 개발 서버에 들어올 수 없습니다.
접속이 끊기거나 앱을 닫으면 전달도 함께 정리됩니다.

> 참고: 절전에서 깨어나 다시 접속하면 판은 자동으로 붙지만 포트 전달은
> 다시 눌러 주셔야 합니다.
