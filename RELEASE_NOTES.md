## Armux Terminal v0.10.7

### 퀵메모 (새 기능)

작업 중에 떠오른 것을 **터미널을 벗어나지 않고 바로 적어 둘 수 있습니다.**

하단바 오른쪽 끝, AI 단추 왼쪽의 **📝** 를 누르거나 **`Ctrl/⌘+M`** 을 누르면
그 자리에서 작은 메모창이 올라옵니다. AI 채팅 팝업과 같은 방식이라 화면 배치를
건드리지 않고, 닫아도 없애지 않고 감추기만 하므로 적던 글이 그대로 남습니다.

- **열면 마지막으로 적던 메모를 그대로 잇습니다.** 그런 메모가 없으면(처음
  쓰거나 그 메모를 지웠으면) 새 메모를 자동으로 만들어 바로 커서를 놓습니다.
- 손을 멈추면 **자동 저장**되고, 닫을 때도 남은 글을 저장합니다.
  `Ctrl/⌘+S` 는 즉시 저장.
- 머리의 **메모 이름 ▾** 으로 다른 메모로 갈아타고(목록에서 **✕** 로 삭제),
  **+** 로 새 메모를 만듭니다.
- **⏱** (또는 `Ctrl/⌘+D`) 로 커서 자리에 지금 시각을 넣습니다.
- 가장자리를 끌어 **크기 조절**, **⛶** 로 전체보기. 크기는 기억해 둡니다.
- AI 채팅 팝업이 함께 열려 있으면 **그 왼쪽에 나란히** 서서 서로 가리지
  않습니다.
- `Esc` 로 닫기. 메모창 안에서는 `Ctrl/⌘+W`(판 닫기) 같은 앱 단축키가 끼어들지
  않습니다 — 글을 쓰는 자리이기 때문입니다.

저장 위치는 **메모장 탭과 같습니다**(`<앱 데이터>/notes/*.md`). 급히 적어 둔
것을 **메모장에서 열기** 로 넘겨 큰 화면에서 이어 쓸 수 있고, 메모장 탭 목록에도
바로 나타납니다. 도움 메뉴와 **단축키 모음** 도움말에도 항목을 넣었습니다.

**알아 두실 점**

- 터미널에서 `Ctrl+M` 은 원래 `^M`(Enter)입니다. 이제 앱이 가로채므로 터미널로
  전달되지 않습니다. Enter 는 그대로 동작합니다.
- 맥에서 `⌘M` 은 보통 "창 최소화" 인데, Armux 안에서는 퀵메모가 우선합니다.
  최소화는 창 왼쪽 위 노란 단추로 하시면 됩니다.
