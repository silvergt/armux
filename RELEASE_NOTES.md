## Armux Terminal v0.10.9

### 탭 표시가 이제 tmux 의 모든 창을 봅니다 (스피너 추가)

**안 보고 있는 tmux 창에서 백테스트가 돌면 탭에 스피너가 돌고, 안 보는 사이에
끝나면 초록 느낌표가 뜹니다.** 창을 옮기거나 tmux 에서 빠져나가도 표시가 따라
옵니다.

지금까지는 화면을 읽어 추측하거나 훅이 쏘는 신호 하나에 기대고 있었습니다. 둘 다
"바뀌는 순간" 만 잡는 방식이라, 한 번 놓치면 다음 신호가 올 때까지 계속 틀린
상태로 남았습니다. 특히 tmux 는 창을 전환할 때 **글자만 다시 그리고 지나간 신호를
다시 쏘지 않기 때문에**, 창을 옮기는 순간 표시가 어긋났습니다.

방향을 뒤집었습니다. 접속마다 채널 하나를 열어 두고 **2초마다 서버에 직접**
"어느 창이 보이고, 각 창에서 무엇이 돌고 있나" 를 물어봅니다. 무엇을 놓쳐도 다음
조회에서 맞춰지므로 틀린 표시가 2초 넘게 남지 않습니다.

### Claude 신호가 다른 창에서는 통째로 사라지던 문제

**Claude 를 지금 보고 있지 않은 tmux 창에서 돌리면, 작업이 끝나도 초록 느낌표가
안 떴습니다.** 안 보는 창의 완료를 알리는 것이 이 기능의 존재 이유인데, 정확히
그 상황에서만 조용히 실패하고 있었습니다.

원인은 tmux 통과 설정을 켜면서 **대상 창을 지정하지 않은 것**이었습니다. 그래서
설정이 엉뚱하게 "지금 보이는 창" 에 걸리고, 정작 Claude 가 있는 창은 계속 꺼진
채로 남아 신호가 tmux 에 삼켜졌습니다. 창을 한 번 쳐다보면 다음 신호부터
되살아나서, "될 때도 있고 안 될 때도 있는" 것처럼 보였습니다.

신호에 **창 이름표**를 붙여 창마다 상태를 따로 기억하도록 함께 고쳤습니다. Claude
가 완료 신호를 못 보내고 죽어도(크래시·`kill`) 스피너가 남지 않습니다.

### 사람을 기다리는 것은 "돌고 있다" 로 보지 않습니다

| 화면에 있는 것 | 표시 |
| --- | --- |
| `python3 backtest.py`, `pytest`, `rsync`, `make` | **스피너** |
| 셸 프롬프트 | 없음 |
| `vim` · `htop` · `less` · 중첩 `ssh` | 없음 |
| `python3` 혼자 (REPL) | 없음 |
| `git log` (페이저가 기다리는 중) | 없음 |
| `sudo -i` 로 연 루트 셸 | 없음 |
| 그 루트 셸 **안에서** 돌린 명령 | **스피너** |
| `tail -f` 로 로그 감시 | 없음 |

이름만으로 가를 수 없는 것은 **명령줄 인자와 자식 프로세스까지** 봅니다.
`python3` 혼자면 REPL, `python3 bt.py` 면 작업. `sudo -i` 는 그 아래 셸까지
따라가 보고 판단합니다.

모르는 것은 "작업 중" 이 아니라 "판단 안 함" 쪽으로 둡니다 — 틀린 스피너보다
**틀린 완료 알림**(보러 갔는데 안 끝나 있는 것)이 훨씬 나쁘기 때문입니다.

초록 느낌표도 **못 본 것에만** 뜹니다. 눈앞에서 `Ctrl+C` 로 멈춘 것에는 뜨지
않고, 안 보이는 창에서 끝났을 때만 뜹니다.

### 서버에 부담이 없습니다

포그라운드 명령을 알아낼 때 `ps` 로 전체 프로세스를 훑으면 17ms 가 드는데,
`/proc` 에서 필요한 파일만 읽으면 0.5ms 입니다. 한 조회가 8ms 남짓, **접속당
CPU 0.4%** 정도입니다. `/proc` 이 없는 서버(맥·BSD)는 `ps` 로 폴백합니다.

### 아직 안 되는 것

- **중첩 tmux**(tmux 안에서 또 tmux)·**중첩 ssh**(붙은 서버에서 또 다른 서버로
  ssh) 안쪽은 판단하지 않습니다. 바깥에서는 프로그램 하나로만 보입니다. 스피너가
  안 뜰 뿐, 잘못된 알림은 뜨지 않습니다.

---

판정 규칙 46가지 조합을 `npm run test:panestate` 로 검증합니다.
