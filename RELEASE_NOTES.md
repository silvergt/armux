## Armux Terminal v0.11.1

### 설정 창이 생겼습니다 (정보 ▸ 설정)

세 갈래입니다.

**모양**

| 항목 | 값 |
| --- | --- |
| 테마 | 어둡게 · **밝게** · 시스템(OS 설정을 따라감) |
| 글자 크기 | 8~28 |
| 글꼴 | 비워 두면 기본 글꼴 |
| 커서 모양 | 블록 · 줄 · 밑줄 |
| 커서 깜빡임 | 켬/끔 |
| 스크롤백 | 1,000~200,000줄 |

**단축키**

- **메인탭 ↔ 서브탭 번호 단축키 바꾸기** — 기본은 `Ctrl/⌘+숫자` 가 서브탭(가로 줄),
  `Ctrl+Alt/⌘⌃+숫자` 가 메인탭(세로 열)입니다. 스위치 하나로 맞바꿉니다. 지금 어느
  쪽인지 바로 아래에 글로 적어 줍니다.
- 목록의 단축키는 **눌러서 새 키를 지정**합니다(↺ 로 기본값 복귀). 바꾸면 **예전 키는
  더 이상 듣지 않습니다.** 다른 동작이 쓰던 키를 지정하면 그쪽은 비워집니다.
- 맥에서는 시스템 메뉴가 키를 먼저 가져가므로, 메뉴의 단축키도 함께 다시 세웁니다.
  이걸 안 하면 렌더러에서만 바뀌고 옛 키가 계속 살아 있습니다.
- 복사·붙여넣기처럼 터미널 입력과 얽힌 키는 바꿀 수 없게 두었습니다.

**동작** — 작업 완료 알림 · 절전에서 깨면 자동 재접속 · 재접속하면 tmux 다시 붙기 ·
뽀모도로 기본 시간.

`기본값으로` 를 누르면 설정과 단축키가 한 번에 처음 상태로 돌아갑니다.

### 밝은 테마

색이 176종·403곳에 직접 박혀 있어 손으로 옮기면 반드시 빠뜨리는 곳이 생깁니다.
그래서 원본 스타일에서 **기계로 만들어 냅니다** — 무채색은 밝기를 뒤집고, 강조색은
색상과 채도는 두고 밝기만 뒤집되 흰 바탕에서 읽히는 범위로 가둡니다. 그림자는 옅게,
흰 덧칠은 검정 덧칠로 바꿉니다. 터미널 안쪽 색도 함께 바뀝니다.

기계로 안 되는 몇 군데(색이 칠해진 단추 위의 흰 글씨 등)는 따로 손질해 두었지만,
**처음 내는 밝은 테마라 어색한 구석이 남아 있을 수 있습니다.** 눈에 걸리는 곳이
있으면 알려 주세요.

### 뽀모도로

- 아이콘을 토마토에서 **시계**로 바꿨습니다. 작은 크기에서도 시계로 읽히도록 채우기가
  아니라 선으로 그렸습니다.
- 시간 입력 창이 **투명해서 뒤가 비치던 문제**를 고쳤습니다. 배경색으로 존재하지 않는
  값을 쓰고 있어 아예 칠해지지 않고 있었습니다.
