## Armux Terminal v0.11.9

### 터미널 색 — 어둡게 / 밝게 (설정 ▸ 모양)

Claude Code·Codex 에서 `/config` 테마를 light 로 바꿔도 색이 바뀌지 않던 문제를
해결했습니다. 원인은 **armux 가 터미널 배경을 검은색으로 고정**하고 있었고, 그걸
바꿀 설정이 없었기 때문입니다(v0.11.2 에서 밝은 테마를 걷어낼 때 터미널 팔레트
선택지까지 같이 빠졌습니다).

확인해 보니 Claude 의 라이트 테마 자체는 잘 동작하고 있었습니다 — 라이트에서는
검은 글자·흰 강조를 실제로 쓰고 있었는데, **판 배경만 검은색**이라 글자가 묻혔던
것입니다. 터미널 배경은 터미널이 소유하므로 어떤 앱도 바꿀 수 없습니다(Claude 는
`OSC 11` 로 "네 배경색이 뭐냐" 고 묻기만 합니다 — 어떤 터미널에서도 같습니다).

**설정 ▸ 모양 ▸ 터미널 색** 에서 어둡게 / 밝게를 고릅니다(맥은 `⌘,`).

- **터미널 판 안쪽 색만** 바뀝니다 — 탭바·설정창 같은 앱 UI 는 어두운 채로 둡니다
- 열려 있는 판에 곧바로 적용되고, 다음 실행에도 유지됩니다
- 판 여백과 검색 강조색도 함께 따라갑니다
- 밝게로 두면 armux 가 배경 질의에 "흰색" 이라고 답하므로, Claude·Codex 테마를
  `auto` 로 두어도 라이트가 자동으로 잡힙니다

---

앱만 올리면 반영됩니다(서버 쪽 변경 없음).
