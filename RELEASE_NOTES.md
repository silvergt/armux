## Armux Terminal v0.10.8

### 맥에서 한글이 씹히던 문제 (원인: 지난 버전에서 넣은 수정 코드)

한글을 이어서 치면 **글자가 통째로 사라지고, 스페이스를 눌러도 띄어쓰기가 안
되던** 문제를 고쳤습니다. 원인은 v0.10.6 에서 "한글 입력 수정" 이라며 넣은
코드 자체였습니다.

xterm 은 조합이 끝나면 잠시 뒤에 **숨은 입력칸의 값을 잘라내서** 터미널로
보냅니다. 그런데 그 코드가 조합이 끝날 때마다 그 입력칸을 비워 버려서, 빠르게
치면 앞 글자를 보내기도 전에 지워졌습니다. 실제로 재현해 보면:

| 친 것 | v0.10.6~0.10.7 | v0.10.8 |
|---|---|---|
| 한글 | `한` | `한글` |
| 한글 안녕 | `한 안 ` | `한글 안녕 ` |
| 한글안녕다서 | `한` | `한글안녕다서` |

그 코드를 걷어냈습니다. 이제 입력칸은 xterm 이 알아서 관리하고 앱은 손대지
않습니다.

**원래 잡으려던 "너너너" 도 함께 막았습니다.** 그 버그의 진짜 원인은 브라우저가
숨은 입력칸의 커서를 옮기는 것이었는데, 커서를 옮길 수 있는 키 31가지를 모두
확인해 **맥의 ⌘↑ / ⌘↓ / ⌘← / ⌘→ 가 막히지 않고 있던 구멍**을 찾아 닫았습니다.

같은 일이 세 번 반복되지 않도록 **한글 입력 회귀 시험을 저장소에 넣었습니다**
(`npm run test:ime`). 코드를 되돌리면 시험이 바로 실패합니다.

### 맥에서 ⌘V 붙여넣기가 안 되던 문제

접속 창·메모장 같은 입력칸에서 **⌘V 가 아무 반응이 없던** 문제를 고쳤습니다.

맥은 ⌘V 를 메뉴가 먼저 가져가고, 앱은 그 신호를 받아
`document.execCommand('paste')` 를 불렀는데 — 크로미움은 웹 내용이 스스로
붙여넣는 것을 막아 두어서 이 호출은 조용히 아무 일도 하지 않습니다. 오류도 없이
무시됐습니다. 이제 붙여넣기·복사·잘라내기를 네이티브 쪽에서 처리합니다.

같은 이유로 **맥에서는 스크린샷 ⌘V 업로드(v0.10.6)도 동작하지 않았습니다.**
그 경로에도 그림 처리를 넣었습니다.

### 인터넷이 잠깐 끊겨도 세션이 죽지 않습니다

전에는 인터넷이 **2분 20초** 넘게 끊기면 세션이 죽고, Enter 를 눌러 새로
접속해야 했습니다. 이제 두 겹으로 막습니다.

**1) 버티는 시간을 10분으로 늘렸습니다.** 이 안에서 인터넷이 돌아오면
재접속이 아니라 **하던 작업이 그대로 이어집니다.** 끊긴 동안 친 명령도 돌아온
뒤 그대로 실행됩니다. (실측: 5분 끊김 → 같은 셸 그대로)

**2) 그래도 끊어졌으면 Enter 없이 스스로 다시 붙습니다.** 판을 죽이지 않고
주황 점으로 기다리다가 2 → 4 → 8 → 15 → 30초 간격으로 다시 시도합니다(30분까지).
Enter 를 누르면 지금 바로 시도합니다. 화면과 스크롤백은 그대로 남습니다.

붙고 나면 **끊기기 전에 보고 있던 tmux 세션으로 돌려놓습니다.** 서버의 tmux 는
살아 있으므로 화면도, 돌아가던 명령도 그대로입니다.

`exit` 로 끝낸 세션과 인증에 실패한 접속은 다시 붙지 않습니다.
