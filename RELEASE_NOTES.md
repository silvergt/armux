## Armux Terminal v0.10.1

### ◆ Codex 지원 (새 기능)

서버에 Codex CLI 가 깔려 있으면 Claude 와 나란히 쓸 수 있습니다. 무엇이
깔려 있는지는 접속할 때 알아서 확인합니다.

- **작업이 끝나면 초록 느낌표.** Claude 와 똑같이 codex 도 턴이 끝나거나
  승인을 기다릴 때 알려 줍니다. (`~/.codex/config.toml` 의 `notify` 를 씁니다.
  이미 쓰고 계신 `notify` 가 있으면 건드리지 않습니다)
- **하단바에 codex 사용량**을 초록 막대로 표시합니다.
- **AI 채팅에서 Claude ↔ Codex 선택.** 기본은 Claude 이고, 그 서버에 실제로
  깔려 있는 것만 목록에 뜹니다.

### ✳ AI 채팅으로 통합 · 답변 서식

따로 있던 "AI 질문" 창을 없애고 AI 채팅으로 합쳤습니다.

- **`Ctrl/⌘+K`** 는 이제 별도 창이 아니라 **지금 판 아래를 쪼개** AI 채팅을
  엽니다. 그 판의 내용(터미널 화면·드래그로 고른 글자·웹 본문·열어 둔 파일)이
  자동으로 첨부됩니다. 같은 서브탭에 채팅 판이 있으면 그대로 이어 씁니다.
- **답변이 서식대로 보입니다.** 코드블록은 문법 색칠 · 언어 표시 · 복사 버튼과
  함께 나오고, 목록 · 표 · 굵게 · 인용도 제대로 그려집니다. 예전에는 ``` 같은
  기호가 날것으로 보였습니다.
- **새 대화** 를 누르면 지금 대화는 화면에서 사라지고 **기록**으로 넘어갑니다.
  머리의 "기록" 에서 지난 대화를 다시 꺼내 볼 수 있습니다.
- 📎 컨텍스트 버튼이 눌리지 않던 문제를 고쳤습니다.

### 알림과 복구

- **창이 가려져 있어도 알림이 옵니다.** 작업이 끝나거나 입력을 기다리면 OS
  알림이 뜨고, 누르면 창이 앞으로 나오면서 **그 판으로 바로 이동**합니다.
  대기 건수는 독 배지(맥·리눅스) / 작업표시줄 깜빡임(윈도우)으로 보입니다.
- **절전에서 깨면 끊긴 판을 알아서 다시 붙입니다.** 노트북을 덮었다 열 때마다
  판마다 Enter 를 누르지 않아도 됩니다. 네트워크가 복구될 때도 같습니다.
  다시 붙을 때 그 판이 보고 있던 **tmux 세션으로 돌려놓습니다**.
- 보기 메뉴에서 위 세 가지를 각각 끌 수 있습니다.

### 새 판 · 파일 탐색기

- **판을 나누거나 서브탭을 추가하면 "무엇을 열까요?"** 를 먼저 묻습니다.
  터미널 · 웹페이지 · AI 채팅 · 메모 · 파일 중에서 ↑↓·Enter·숫자키·마우스로
  고릅니다(터미널이 기본). 웹페이지만 보려는데 셸이 하나씩 붙던 낭비가 없어집니다.
- **파일 탐색기를 미리 붙여 둡니다.** 접속하면 곧바로 SFTP 를 열어 두므로
  📁 를 눌렀을 때 기다리지 않습니다.
- 접속 창의 **"로컬 터미널"** 버튼을 위쪽 탭으로 옮겼습니다.

### 고친 것

- **여러 줄로 잘린 긴 링크가 첫 줄만 열리던 문제.** codex 로그인 주소처럼
  프로그램이 자기 폭으로 잘라 여러 줄에 그린 링크도 하나로 인식해 전체가
  열립니다. 3,000자가 넘어 수십 줄이 되어도 잘리지 않습니다.
- **작업을 시작하자마자 초록 느낌표가 뜨던 문제.** 이제 정말 끝났을 때만 뜹니다.
- **웹 링크에서 "이 링크를 열 앱이 없습니다" 대화가 뜨던 문제.** 스크립트가
  채우는 빈 팝업은 크롬처럼 실제 팝업 창으로 열립니다.
- 메인탭 번호 배지에서 `Alt`·`⌘⌃` 표기를 빼고 숫자만 남겼습니다(단축키는 툴팁에).
